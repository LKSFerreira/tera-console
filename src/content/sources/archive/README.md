# Arquivo de rascunhos NÃO publicados

Conteúdo gerado por ingest automático que **falhou o padrão de qualidade B131**.
Não é carregado pelo portal (`carregarPatches` só lê `content/patches` + dataDrivenIds publicados).

Não promover daqui para `content/patches` sem curadoria completa.
